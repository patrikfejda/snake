function drawSnake(tileCountX, tileCountY, tileSize, ctx) {
    let i = 2;
    let j = 2;
    // for (let i = 0; i < tileCountX; i++) {
    //     for (let j = 0; j < tileCountY; j++) {
            ctx.fillStyle = "#906090";
            ctx.fillRect(tileSize * i, tileSize * j, tileSize - 1, tileSize - 1);
    //     }
    // }
    
}